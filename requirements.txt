# CI / runtime (GitHub Actions + local gate)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
mcp>=1.27,<2
pydantic>=2.0
boto3>=1.35

# Dev / test (optional: pip install -r requirements-dev.txt)
# pytest>=8.0
# pytest-asyncio>=0.24
# httpx>=0.27
