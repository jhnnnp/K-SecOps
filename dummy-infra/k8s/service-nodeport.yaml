apiVersion: v1
kind: Service
metadata:
  name: payment-api-nodeport
  namespace: default
spec:
  type: NodePort
  selector:
    app: payment-api
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
